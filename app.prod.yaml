version: "3.9"
services:
    dashboard:
        build:
            context: .
            dockerfile: Dockerfile-prod
        volumes:
            - ./.env:/app/.env
        ports:
            - 5000:5000
        command: npm run start
